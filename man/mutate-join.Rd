% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{mutate-join}
\alias{mutate-join}
\alias{inner_join.DataFrame}
\alias{left_join.DataFrame}
\alias{right_join.DataFrame}
\alias{full_join.DataFrame}
\title{Mutating joins}
\usage{
\method{inner_join}{DataFrame}(x, y, by = NULL)

\method{left_join}{DataFrame}(x, y, by = NULL)

\method{right_join}{DataFrame}(x, y, by = NULL)

\method{full_join}{DataFrame}(x, y, by = NULL)
}
\arguments{
\item{x}{a \code{DataFrame}}

\item{y}{a \code{DataFrame} or \code{data.frame}}

\item{by}{columns to use for joining the objects. If \code{NULL}, the function will look for common columns.}
}
\value{
a \code{DataFrame}
}
\description{
Mutating joins
}
\examples{
da <- starwars[, c("name", "mass", "species")][1:10, ]
db <- starwars[, c("name", "homeworld")]

Da <- as(da, "DataFrame")
Db <- as(db, "DataFrame")

Res_inner <- inner_join(Da, Db[1:3, ])

}
