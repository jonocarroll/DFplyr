.onLoad <- function(libname, pkgname) {
  # op <- options()
  # op.DFplyr <- list(
  #   DFplyr.print_as = "DataFrame"
  # )
  # toset <- !(names(op.DFplyr) %in% names(op))
  # if (any(toset)) options(op.DFplyr[toset])

  invisible()
}
